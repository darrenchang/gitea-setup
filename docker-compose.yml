services:
  web-server:
    image: localhost/gitea-serup/web-server:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    networks:
      - gitea
    depends_on:
      - server

  server:
    image: docker.gitea.com/gitea:1.26.1
    container_name: gitea
    env_file: .env
    environment:
      - USER_UID=${USER_UID}
      - USER_GID=${USER_GID}
      - GITEA__database__DB_TYPE=postgres
      - GITEA__database__HOST=db:5432
      - GITEA__database__NAME=${DB_NAME}
      - GITEA__database__USER=${DB_USER}
      - GITEA__database__PASSWD=${DB_PASSWORD}
    restart: always
    networks:
      - gitea
    volumes:
      - gitea_data:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "222:22"
    depends_on:
      - db

  db:
    image: docker.io/library/postgres:14
    restart: always
    env_file: .env
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    networks:
      - gitea
    volumes:
      - postgres_data:/var/lib/postgresql/data

networks:
  gitea:
    external: false

volumes:
  gitea_data:
  postgres_data:
